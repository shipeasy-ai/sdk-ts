# Resolve plain-semver deps on sibling workspace packages to the local source
# (pnpm 10 defaults this off). Lets the published manifests carry real semver
# ranges — `npm publish`, unlike `pnpm publish`, does NOT rewrite `workspace:`
# specifiers, and the release workflow uses npm for OIDC trusted publishing.
link-workspace-packages=true
