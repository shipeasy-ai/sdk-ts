{
  "name": "shipeasy-entity-guide",
  "version": "0.1.0",
  "private": true,
  "description": "A single-page visual guide to every Shipeasy SDK entity (flags, configs, experiments, kill switches, events, i18n, errors). Standalone — no @shipeasy/sdk wired in yet.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run"
  },
  "dependencies": {
    "next": "15.5.4",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@shipeasy/sdk": "file:../..",
    "@types/node": "22.10.7",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "typescript": "5.7.3",
    "vitest": "^2.1.0"
  }
}
